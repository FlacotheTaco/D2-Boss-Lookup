import React from "react";
const Info = () => {
    return (
    <div className="info">
        <p> Type the boss name exactly as it appears on the list below</p>
    </div>
    )
}
export default Info;